# UGC-2b-Match16 构建清单（2026-09-16 开闸）

> 上位：`ugc-2b-match16-scope-draft.md`（已确认 · 开闸）  
> 约束：不跑全量测试；禁止未确认灌库；密钥仅服务端

## In
1. 宫格入册入口显式二选一：「AI 切图」｜「手动四宫/九宫」（不得仅藏高级）
2. AI：取消产品「最多 16 张」；检测/提交默认技术安全上限 **64**（`GRID_VLM_MAX_DETECT` / `GRID_VLM_MAX_SUBMIT` 可覆盖）；仅超出安全上限时截断最高置信 + Toast。手动 4/9 不变。
3. 确认切图后每卡匹配 published：命中 → 直接挂拥有；未命中 → UGC-1 pending 审批
4. 版本字段可选，空值可提交
5. 建议文案可预填须确认；禁止自动 published 文案
6. OQ 默认：匹配阈值沿用；多候选 Top1；命中不记审批积分；手动仍仅 4/9

## Out
未确认灌库、端侧 VLM、H5 宫格、短信登录、交易、强制填版本

## 验收
见 freeze §5（入口二选一、AI 无产品张数上限、命中无 pending、未命中进审、版本可空）

## 部署
合入后部署 api + 小程序拉 main（Admin 本切可不改除非要展示匹配入册标记）
