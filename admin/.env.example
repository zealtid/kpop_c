# Build-time API origin for the Admin SPA (Vite). No trailing slash.
# Local `npm run dev` proxies /admin to VITE_API_PROXY (default http://127.0.0.1:3000)
# and can leave this empty. Production / `npm run build` must set it.
VITE_API_BASE=https://api-production-0818.up.railway.app
